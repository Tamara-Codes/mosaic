{
  "buildCommand": "cd frontend && npm run build && mkdir -p public && cp -r frontend/dist/* public/",
  "installCommand": "pip install -r requirements.txt --break-system-packages && cd frontend && npm install"
}